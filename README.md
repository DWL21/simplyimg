# simplyimg

Monorepo scaffold for a browser-first image editing service with:

- `packages/img-processor`: Rust -> WASM image processing engine
- `apps/worker`: Cloudflare Worker fallback API
- `apps/web`: React + Vite frontend
